# Secrets — never commit
.env.local
.env*.local

# Local database and its WAL/SHM sidecar files
data/*.db
data/*.db-wal
data/*.db-shm

# On-disk backups (exported financial data)
data/backups/

# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/
tsconfig.tsbuildinfo

# Logs and OS cruft
*.log
.DS_Store

# Uploaded source documents (private financial PDFs)
uploads/
*.pdf
