/**
 * GoCardless bank-balance sync (rule 2.4 permitted outbound call).
 * Phase 2 stub. Verify UAE bank support before relying on this.
 */

export async function syncBalances(): Promise<void> {
  throw new Error("syncBalances: not implemented (Phase 2)");
}
